# مركز المعرفة والابتكار - خطة البناء

> ⚠️ ملاحظة: القائمة أدناه قديمة ولا تعكس الوضع الحالي — المشروع الآن فيه 40 قسم فعلي (شوف `app/` مباشرة، تحديث 2026-09-04). راجع "آخر التحديثات" تحت لآخر عمل حقيقي تم.

## 🔒 إصلاح جذري: تعليق صامت أبدي على سفاري/شبكات بطيئة + إصلاح خطر بالمسابقة الحية (2026-09-05، commits `f45cedc` و`359f8af`)
شكوى متكررة من محمد: سفاري والمنسّقون على اللابتوب "ما تفتح المنصة". السبب الجذري الحقيقي (مو مجرد "سفاري سيء"): `ensureSignedIn()` (تسجيل الدخول المجهول التلقائي، يصير قبل أي عملية Firebase) كان بلا أي حد زمني، وبما إنه وعد مشترك (`signInPromise`) يُعاد استخدامه بكل نداء، أي تعليق صامت لمرة واحدة يُعطّل كل عمليات المنصة للأبد بلا أي خطأ ظاهر.
- **الإصلاح**: حد 10 ثوانٍ على `ensureSignedIn` + تصفير الوعد عند الفشل، نفس الحماية على `cloudSet`/`cloudPush`/`cloudTransact` (كانت بلا حد زمني للكتابة)، و`cloudListen` صار عنده شبكة أمان (قراءة احتياطية بعد 10 ثوانٍ لو ما وصلت بيانات حية، وكل 20 ثانية بعدها).
- **إصلاح خطر ثانٍ بنفس الجلسة**: `lib/liveQuiz.ts` (مضافة يوم قبلها) كان فيها نمط `current as X` لما `current` يكون `null` داخل معاملة Firebase — فايربيز يتعامل مع `null` المُرجَعة كأمر "احذف هذا المسار"، يعني أي تعطّل لحظي بالقراءة أثناء مسابقة حية فعلية كان يقدر يمسح جلسة/نقاط لاعب حقيقي بالكامل. `cloudTransact` صار يقبل إرجاع `undefined` لإلغاء المعاملة بأمان، واستُخدم بدل الكاست الوهمي بكل دوال `liveQuiz.ts`.
- **تحقّق فعلي شامل**: `tsc --noEmit` + `npm run build` نظيفان 100% (صفر أخطاء بكل المشروع). **اختُبرت المسابقة الحية بالكامل لأول مرة منذ إضافتها** (كانت غير مجرّبة أبداً): جلستين متصفح حقيقيتين (مضيف + طالب) على الإنتاج الحي — إنشاء جلسة، انضمام لحظي، سؤال، إجابة لحظية، كشف الإجابة، لوحة الصدارة (احتساب نقاط صحيح: 803 نقطة لإجابة صحيحة سريعة)، إنهاء المسابقة — كل شي اشتغل بلا أي خلل. بيانات الاختبار (`kc_live_quiz/429150`, `kc_live_quiz_players/429150`) نُظّفت من Firebase فوراً بعد التحقق.
- **فحص قواعد أمان Firebase الفعلية (كونسول مباشرة، لا افتراض)**: `kc_students_contact`/`kc_coordinators_contact` (الجوال/الهوية) محمية فعلياً بقاعدة `auth.token.email === admin فقط` — هذا الجزء الأخطر كان مُصلَحاً فعلاً من جلسة سابقة، تأكّد اليوم بالفحص المباشر. **لسه قائم ومو مُصلَح عمداً**: قاعدة `"$other": {".read": "auth != null", ".write": "auth != null"}` تعطي أي زائر (حتى بحساب مجهول تلقائي) قراءة/كتابة كاملة على كل البيانات الثانية (kc_students بدون الحقول الحساسة، kc_coordinators، نتائج الاختبارات، نقاط المسابقات، المزاد...). لم يُصلح اليوم عمداً — يحتاج تصميم قواعد أدق لكل مسار على حدة بجلسة مخصصة هادئة (مو وسط تسجيل طلاب فعلي)، لتفادي قفل ميزة حية بالغلط. **لو رجعت لهذا لاحقاً**: ابدأ بمسارات القراءة العامة الحقيقية فقط (`kc_reg_codes` تحديداً يحتاج حل بنية مختلف تماماً لأن التحقق من رمز التسجيل يصير بالعميل حالياً — تقييد قراءته يكسر تسجيل المنسقين الجدد فوراً).
- **✅ تجميع تبويبات لوحة الإدارة (commit `2df0547`)**: 33 تبويباً بصف واحد ملتف (نفس المشكلة الموثّقة بجلسة 2026-09-03 — أيقونة المزاد ضاعت وسط الزحمة) صاروا مجمّعين بـ5 فئات (`NAV_GROUPS` بـ`app/admin/page.tsx`): الطلاب والمنسقون، التقييم والتقدم، المحتوى التعليمي، الأنشطة الحية، عام. **تجميع بصري بس** — نفس المعرّفات، نفس منطق العرض، صفر تغيير بالسلوك. تحقّق آلي (سكربت مطابقة) إن الـ33 كلهم موجودون بلا نقص/تكرار + `tsc`/`build` نظيفان + **تحقّق بصري حي على الإنتاج** (لقطات شاشة + ضغط تبويب "مزاد المشاريع" من فئة مختلفة وتأكيد التنقل شغّال 100%).
- **الحالة الآن (نهاية جلسة 2026-09-05)**: من نقاط الضعف الخمس المذكورة بتقييم "٨/١٠"، بقيت نقطة واحدة فقط: قواعد أمان `$other`. الباقي (تأكيد حماية الاتصال، اختبار المسابقة الحية، تنظيم تبويبات الإدارة) ✅ منجز ومتحقَّق منه فعلياً.

## 🔒 إصلاح ثغرة أمان حقيقية: تسريب رقم الهوية الوطنية لكل الطلاب (2026-09-04)
اكتُشف سابقاً وتُرك بدون حل: أي زائر للموقع (حتى بدون تسجيل دخول حقيقي — التطبيق يسجّل كل زائر "مجهول" تلقائياً بمجرد فتح الصفحة) كان يقدر تقنياً يقرأ **رقم الهوية الوطنية** لكل طالب مسجَّل عبر طلب مباشر لقاعدة البيانات. جوال الطالب/ولي الأمر كانا محميين مسبقاً بجلسة سابقة (عقدة `kc_students_contact` مقروءة للأدمن فقط)، لكن رقم الهوية بقي مكشوفاً لأنه كان يُستخدم كمعرّف بحث مباشر (تسجيل دخول قديم، بوابة ولي الأمر).

**الحل المطبَّق (بدون الحاجة لخدمة سحابية مدفوعة/Cloud Functions):**
- رقم الهوية انتقل لنفس عقدة `kc_students_contact` المحمية (أدمن فقط)، جنب الجوال.
- فهرس بحث عام جديد `kc_nid_index` (هاش SHA-256 لرقم الهوية ← معرّف الطالب) — يسمح بإيجاد طالب لمن يعرف رقم هويته مسبقاً (تسجيل الدخول، بوابة ولي الأمر، فحص التكرار عند التسجيل) بدون تمكين أي زائر من "تحميل" قائمة كل الطلاب دفعة واحدة.
- **تنويه أمانة مهم**: هذا تقليل جوهري وحقيقي للخطر (من "تسريب فوري لكل الطلاب بطلب واحد" إلى "يحتاج مهاجم محترف يملك موارد حوسبة لكسر الهاش بالقوة الغاشمة")، **مو حماية مطلقة 100%** — رقم الهوية السعودي 10 أرقام (مجال محدود نسبياً)، فكسر الهاش نظرياً ممكن لمهاجم جاد. الحل الكامل المضمون رياضياً يحتاج خدمة سيرفر حقيقية (Firebase Cloud Functions) وهذا يتطلب ترقية مشروع Firebase لخطة مدفوعة (Blaze) — **قرار محمد وحده**، لم يُنفَّذ اليوم.
- **إصلاح إضافي حرج اكتُشف أثناء الفحص**: دالة `updateProfile` (تعديل الطالب لبياناته الشخصية بنفسه) كانت تكتب بياناته كاملة (جوال/هوية) رجوعاً للنسخة العامة غير المحمية في كل مرة يعدّل فيها ملفه الشخصي — يعني الحماية كانت "تتكسر تلقائياً" أول ما أي طالب يحدّث صورته أو بياناته. أُصلح ليمرّ بنفس آلية الفصل المحمية.
- **رُحّلت بيانات الإنتاج الحية فعلياً** (لا مجرد كود جاهز غير مفعّل): سكربت مؤقت (أُنشئ ونُفّذ وحُذف فوراً، لم يُترك بالمستودع) رحّل **28 من 28 طالب مسجَّل فعلياً** كانوا لسه مكشوفين بالكامل (تأكيد أن هذا لم يكن مجرد خطر نظري، بل تسريب فعلي مستمر منذ أول تسجيل) — تحقّق نهائي بعد الترحيل أكّد **صفر** سجلات لسه مكشوفة. نُسخة احتياطية محلية كاملة من البيانات قبل أي تعديل محفوظة على جهاز محمد قبل التنفيذ.
- **اختُبر**: `tsc --noEmit` نظيف، الترحيل تحقّق آلياً بعد التنفيذ (`stillLeakingAfterFix: 0`).
- **لم يُلمس اليوم عمداً**: بريد المنسّقين (لسه بالنسخة العامة — أقل حساسية بكثير من هوية طالب قاصر، ويُستخدم لتسجيل دخولهم، لم يذكره محمد صراحة كخطر).

## ✅ مزاد مشاريع الطلاب (`/auction`) — ميزة جديدة كاملة (2026-09-03)
محمد طلب نظام مزاد حقيقي: طلاب ينفّذون مشروعاً (مثال: جهاز اكتشاف الغاز في المطابخ)، يُعرض بصورة/فيديو باسم الطالب، تُعرض تكلفة التنفيذ للشفافية، ويفتح باب المزاد بسعر افتتاحي، والأهالي يرفعون السعر بمقدار ثابت (افتراضياً 10 ريال) حتى الإغلاق — الفائز يتواصل معه المشرف يدوياً لتوقيع عقد بيع/شراء وتسليم المنتج (**خارج المنصة تماماً — لا دفع إلكتروني، لا عقود آلية**، هذا قرار مقصود لتفادي أي تعقيد قانوني/تسجيل تجاري).

**القرارات المعمارية:**
- **صفحة عامة `/auction`** بدون تسجيل دخول (نفس نمط `/interest-survey`) — رابط واحد يُشارك بواتساب، أي حد يفتحه يشارك.
- **هوية خفيفة بدل حساب كامل**: أول مزايدة، الزائر يكتب اسمه ورقم جواله مرة وحدة (يُحفظ محلياً بمتصفحه بـlocalStorage) — يكفي عشان نعرف نتواصل مع الفائز، بدون قفل التسجيل الكامل.
- **خصوصية المزايدين**: كل الزوار يشوفون السعر الحالي بس، ما يشوفون مين رفعه — الأدمن بس يشوف كل الأسماء والأرقام وسجل كل مزايدة.
- **منع "القنص" بآخر لحظة**: لو جت مزايدة بآخر دقيقتين من الإغلاق، يمدد المزاد تلقائياً دقيقتين إضافيتين (نفس نمط أمازون/eBay) — **اختُبر حياً وأكّد الامتداد فعلياً**.
- **أمان التزامن**: رفع السعر يستخدم معاملة Firebase حقيقية (`runTransaction`) بدل قراءة-ثم-كتابة — يمنع ضياع مزايدة لو اثنان ضغطوا بنفس اللحظة بالضبط.
- **بدون سيرفر/cron**: حالة "المزاد بدأ/انتهى" تُحسب لحظياً بمقارنة الوقت الحالي بتاريخ البداية/النهاية المخزّن — لا حاجة لخدمة خلفية.
- فيديو المشروع (اختياري) يُرفع مباشرة كملف (base64 بحد 15 ميجا) — نفس نمط رفع الفيديو بمساحة الحديث الجماعية، بطلب محمد صراحة (مو رابط يوتيوب).

**الملفات:**
- `lib/auction.ts` — الأنواع + عمليات Firebase (`getAuctions`, `saveAuctions`, `placeBid` بمعاملة حقيقية، `markDelivered`).
- `app/auction/page.tsx` — الصفحة العامة (بطاقات المزادات، عدّاد تنازلي حي، زر رفع السعر، تسجيل هوية خفيف).
- `components/admin/AuctionAdmin.tsx` — إدارة المزادات (إنشاء مزاد جديد برفع صورة/فيديو وتحديد الأسعار والتواريخ، عرض المتصدر الحالي وسجل كل المزايدات، تعليم "تم التسليم").
- تبويب جديد "مزاد المشاريع" بلوحة الإدارة + `/auction` أُضيفت كصفحة مستقلة بـ`AppShell.tsx` (بدون سايدبار).

**🐛 خطأ حرج اكتُشف وأُصلح أثناء الاختبار الحي الأول**: أول محاولة إنشاء مزاد فشلت بصمت (النموذج قفل وكأنه نجح، لكن Firebase رفض الكتابة) — السبب: `video: form.video || undefined` ينتج قيمة `undefined` حرفية، وFirebase Realtime Database **يرفض أي قيمة `undefined`** بالكتابة (نفس عائلة أخطاء "المصفوفات الفاضية تُحذف" الموثّقة بمشاريع ثانية، بس هذي أشد — ترفض الكتابة كاملة). الإصلاح: تخزين `""` بدل `undefined` لما ما يكون فيه فيديو. **درس مهم إضافي**: `cloudSet`/`cloudGet` بهذا المشروع تبتلع الأخطاء داخلياً (`console.error` بس، بدون `throw`)، فأي كود يفترض إن `await cloudSet(...)` نجح لمجرد ما "ما رمى استثناء" غلط — لازم يتحقق من نجاح الكتابة فعلياً (زي `cloudTransact`/`cloudPush` اللي ترجّع `boolean`) قبل ما يقفل أي فورم بثقة إنه "تم".

**اختُبر حياً بالكامل من الصفر لين النهاية** (مفتاح Groq الحقيقي مو مطلوب هنا، بس Firebase الحقيقي نعم):
1. أنشأت مزاد تجريبي فعلي من لوحة الإدارة (صورة حقيقية مرفوعة، بداية قبل دقيقة، نهاية بعد 3 دقايق).
2. فتحت `/auction` العامة، سجّلت هوية زائر تجريبي، رفعت السعر من 200 إلى 210 ريال.
3. تأكدت العدّاد التنازلي امتد تلقائياً (من ~1:00 إلى 1:49) — تأكيد قاطع إن حماية "منع القنص" تشتغل فعلياً.
4. رجعت للوحة الإدارة، تأكدت ظهور "المتصدر الحالي" واسمه ورقم جواله وسجل المزايدة بدقة تامة.
5. **نظّفت كل بيانات الاختبار من Firebase فوراً** (`kc_auctions` و`kc_auction_state` رجعوا `null`) — نفس قاعدة [[project_zaitoon_academy]].
`tsc --noEmit` + `npm run build` نظيفان.

**🔲 لسه ما جُرّب**: مشهد "مزادين أو أكثر بنفس الوقت" (البنية تدعمه أصلاً — كل مزاد Firebase state مستقل — بس ما اختُبر عملياً بمزادين حقيقيين متزامنين).

## 🚧 قيد التنفيذ: "أدوات الذكاء الاصطناعي" (`/ai-tools`) — بدأت 2026-09-03
محمد طلب تطوير المنصة بإضافة مجموعة أدوات ذكاء اصطناعي للمعلمين (استوحاها من منصة "Infology"، أرسل صور 3 لقطات شاشة). القرار: هوية مستقلة داخل نفس مشروع مركز المعرفة والابتكار (مو مشروع منفصل)، تُبنى أداة بأداة.

**إصلاح بنية تحتية أول شي (كان لازم قبل أي أداة جديدة)**: مفتاح Groq API كان محفوظاً بمتصفح الأدمن فقط (`localStorage`) — يعني أي معلم يفتح المنصة من جهازه ما كانت تشتغل عنده الأدوات الذكية. صار المفتاح يُحفظ بالسحابة (Firebase، مسار `kc_groq_key`) عبر `lib/groq.ts` (`getGroqKey`/`saveGroqKey`/`deleteGroqKey`) — يشتغل الآن لكل معلم من أي جهاز بمجرد ما محمد يحفظه مرة وحدة من لوحة الإدارة (تبويب "رموز التسجيل"). حدّثت `/ai-assistant` (المساعد الذكي الموجود سابقاً) ليستخدم نفس النمط الجديد بدل القديم.

**✅ جاهزة (4 أدوات + الصفحة الرئيسية):**
- [x] `/ai-tools` — صفحة رئيسية (hub) بكروت لكل أداة، هوية بنفسجية متدرجة، تعرض "قريباً 🔒" للأدوات غير المبنية بعد.
- [x] `/ai-tools/lesson-plan` — **مولّد خطة الدرس**: فورم (مادة + صف + موضوع + مدة الحصة + ملاحظات) → يولّد خطة كاملة (أهداف، وسائل، تمهيد، خطوات الدرس بالتوقيت، أسئلة تقويم، واجب) بصيغة منسّقة قابلة للطباعة والنسخ.
- [x] `/ai-tools/question-generator` — **مولّد الأسئلة**: فورم بعدد أسئلة لكل نوع (اختيار من متعدد/صح-خطأ/مقالية) + مستوى صعوبة → بنك أسئلة كامل بإجاباته، مع زر إظهار/إخفاء الإجابات.
- [x] `/ai-tools/worksheet` — **مولّد ورقة العمل**: نفس الفكرة لكن للتمارين (لا أسئلة اختبار)، تصميم ورقة عمل حقيقية (حقول اسم/تاريخ، خطوط فراغ للإجابة)، مفتاح الإجابة مخفي افتراضياً (الورقة تُطبع فاضية للطالب).
- [x] `/ai-tools/mind-map` — **مولّد الخريطة الذهنية**: موضوع مركزي + فروع رئيسية ملوّنة بنقاط فرعية، تصميم بطاقات بدل رسم شجري حقيقي (أبسط وأثبت عبر الطباعة والجوال).
- [x] `lib/subjects.ts` (قائمة مواد مشتركة) + `lib/groq.ts` (دوال مشتركة: `callGroqText`, `extractJson`, تُستخدم بكل الأدوات الأربعة).
- [x] رابط "أدوات الذكاء الاصطناعي" أُضيف بالقائمة الجانبية للأدمن **وللمنسّقين (المعلمين)**.
- [x] كلاس طباعة عام `body.printing-only` + `.print-area` بـ`globals.css` مستخدم بكل الأدوات الأربعة.
- **اختُبر**: `tsc --noEmit` + `npm run build` نظيفان بعد كل أداة. اختبار حي بمتصفح حقيقي لـ3 من 4 أدوات (hub, lesson-plan, question-generator, worksheet) — الفورم يعمل بصرياً، RTL سليم، حالة "بدون مفتاح API" تعطّل زر التوليد بشكل صحيح. أداة الخريطة الذهنية تحقّقت فقط عبر قراءة DOM النصية (`get_page_text`) لخلل مؤقت بأداة تصوير الشاشة أثناء الجلسة (زووم غير طبيعي بنافذة كروم) — المحتوى الفعلي بالصفحة صحيح وكامل ولا أخطاء بالـconsole، لكن ما تأكدت بصرياً 100% من تصميم بطاقات الفروع الملوّنة. **لم يُختبر التوليد الفعلي بأي أداة** لأنه ما فيه مفتاح Groq محفوظ حالياً — يحتاج محمد يضيف مفتاحه من لوحة الإدارة ← رموز التسجيل، ثم يجرب كل أداة ويأكد الشكل والنتيجة.

**✅ أداة خامسة (2026-09-03، نفس الجلسة):**
- [x] `/ai-tools/activity-generator` — **مولّد الأنشطة الصفية**: بدل "مكتبة أنشطة" ثابتة (كانت تحتاج محتوى حقيقي من محمد يوثّقه بنفسه، ما كنت لأختلق أنشطة "جاهزة" وأقدّمها كأنها موثوقة)، حوّلتها لمولّد AI بنفس نمط باقي الأدوات: نشاط صفي كامل (هدف، أدوات، خطوات تنفيذ، فكرة تعديل) حسب الموضوع/نوع النشاط (جماعي/تنافسي/فردي/حركي)/المدة.
- **تصحيح الأوراق بالذكاء الاصطناعي**: أُضيف بصفحة `/ai-tools` كبطاقة "قريباً 🔒" فقط، **لم يُبنَ بعد عمداً**. السبب: يحتاج نموذج ذكاء اصطناعي يقرأ صور (vision)، وبحثت عن معرّف النموذج الصحيح عند Groq فوجدت تضارب حقيقي بين مصادر متعددة (`llama-4-scout`/`llama-4-maverick` وكلاهما ظهر كمُهمَل حديثاً حسب صفحة `console.groq.com/docs/deprecations`، وبدائل من عائلة Qwen غير مؤكدة 100%). كتابة معرّف نموذج خاطئ بالكود يعني الأداة تفشل بصمت لكل معلم يستخدمها بدون طريقة له يشخّص السبب. **قرار**: ننتظر لين يصير عندنا مفتاح Groq حقيقي مفعّل، نتأكد من قائمة النماذج الفعلية عبر `GET https://api.groq.com/openai/v1/models` مباشرة (تعطي القائمة الحقيقية بدون تخمين)، وبعدها نبنيها بثقة.

**✅ أداة سادسة (2026-09-03، نفس الجلسة):**
- [x] `/ai-tools/competition-generator` — **مولّد المسابقات الصفية**: مسابقة صفية كاملة (فرق/فردية/Quiz خاطف/محطات) بقواعدها ونظام احتساب النقاط وتحديد الفائز — يغطي فكرة "مكتبة المسابقات الجاهزة" من صور Infology بنفس منطق مولّد الأنشطة (توليد AI لا محتوى ثابت).
- **اختُبرت الأدوات الست كلها بصرياً بمتصفح حقيقي هذي المرة** (الاتصال رجع بعد انقطاع مؤقت) — الفورمات والألوان والتنقل تظهر صحيحة 100%، وصفحة `/ai-tools` تعرض الست بطاقات + بطاقة "تصحيح الأوراق" كـ"قريباً".

## 🔴 إصلاح حرج: نموذج llama-3.3-70b-versatile كان مُهملاً بالكامل عند Groq (2026-09-03)
محمد حفظ مفتاح Groq حقيقي أول مرة وجرّب "مولّد خطة الدرس" مباشرة — رجع خطأ: *"The model `llama-3.3-70b-versatile` does not exist or you do not have access to it"*. هذا يؤكد شكّي الموثّق أعلاه: Groq فعلاً أهملوا كل عائلة Llama (3.3 وأيضاً 4 Scout/Maverick).
**الحل**: استعلمت `GET https://api.groq.com/openai/v1/models` بالمفتاح الحقيقي (أول مرة نقدر نتأكد من القائمة الفعلية بدل التخمين من مقالات الويب) ولقيت القائمة النشطة الحالية: `qwen/qwen3.6-27b`, `qwen/qwen3.8-27b` (كلاهما vision)، `openai/gpt-oss-120b`/`gpt-oss-20b`، `groq/compound`، `allam-2-7b` (نموذج عربي من SDAIA)، وغيرها.
اختبرت حياً (curl مباشر) نفس الـprompt المستخدم فعلياً بمولّد خطة الدرس على **gpt-oss-120b** (نموذج تفكير — يحرق توكنات "تفكير" مخفية ضخمة، رجع `finish_reason: length` وJSON غير مكتمل حتى بحد أقصى 800 توكن) مقابل **qwen/qwen3.8-27b** (بدون تفكير مخفي، `finish_reason: stop`، JSON كامل وصحيح 100% بـ942 توكن من أصل 3000، أسرع بكثير). **اخترت `qwen/qwen3.8-27b`** وحدّثت `lib/groq.ts` (كان يستخدمه كل الأدوات الست تلقائياً بمجرد التحديث).
**اختُبر حياً بالكامل بعد الإصلاح** (مفتاح Groq حقيقي محفوظ بالسحابة الآن):
- **مولّد خطة الدرس**: طلب "دورة الماء في الطبيعة" لمادة التربية الإسلامية/الصف الأول — النموذج تعرّف على السياق الديني تلقائياً وأنتج خطة كاملة ممتازة فعلاً (آية "وجعلنا من الماء كل شيء حي"، دعاء حقيقي، نشاط حركي عملي مناسب لعمر الطلاب، 3 أسئلة تقويم، واجب منزلي) — جودة محتوى حقيقية عالية، مو حشو.
- **مولّد الأسئلة**: طلب "جدول الضرب" لمادة الرياضيات/الصف الأول — 10 أسئلة اختيار من متعدد صحيحة رياضياً 100%، الإجابة الصحيحة محدّدة بدقة ومعروضة بتظليل أخضر.
- الأداتان الباقيتان (ورقة العمل، الخريطة الذهنية، الأنشطة، المسابقات) تستخدمان نفس الدالة المشتركة `callGroqText` فنفس الإصلاح ينطبق عليهم تلقائياً — لم تُختبر كل واحدة على حدة بتوليد فعلي بعد، لكن الثقة عالية جداً لتطابق النمط.
- **مفتاح Groq الحقيقي**: محفوظ بالفعل بالسحابة (Firebase `kc_groq_key`) — محمد أعطانيه مباشرة بالمحادثة، حفظته بنفسي عبر سكربت Node مؤقت (firebase SDK، توقيع دخول مجهول Anonymous Auth، بدون أي كلمة مرور)، **حذفت السكربت المؤقت فوراً بعد التنفيذ ولم يُكتب بأي ملف متتبَّع بـgit إطلاقاً**.
- الحساب المجاني عند Groq بدون بطاقة بنكية (Free Tier) — لا خطر خصم مفاجئ إلا لو محمد شخصياً أضاف بطاقة لاحقاً لترقية الخطة.

## ✅ خاصية "سجل أعمالي" مكتملة (2026-09-03، نفس الجلسة)
كل الست أدوات صار فيها زر "حفظ بسجلي" (يظهر فقط لمن سجّل دخوله كمعلم/منسّق أو أدمن — عبر `useAiOwner()` بـ`lib/aiHistory.ts`)، يحفظ نسخة نصية كاملة من النتيجة بمسار Firebase خاص بكل معلم (`kc_ai_history/{ownerId}`، حد أقصى 30 عنصر، الأحدث أولاً). صفحة جديدة `/ai-tools/history` تعرض كل المحفوظات مع بادج ملوّن لكل نوع أداة، تاريخ عربي، وأزرار عرض/نسخ/طباعة/حذف. رابط "سجل أعمالي" ظاهر بأعلى صفحة `/ai-tools` لمن سجّل دخوله.
**اختُبر حياً بالكامل بمحاكاة دخول أدمن محلياً** (`localStorage.kc_admin_auth`، بدون لمس كلمة مرور محمد الحقيقية): ولّدت خطة درس حقيقية ("الحروف الهجائية في القرآن الكريم")، ضغطت "حفظ بسجلي"، تأكدت من ظهورها فوراً بصفحة `/ai-tools/history` بكل التفاصيل صحيحة. **نظّفت بيانات الاختبار من Firebase فوراً بعدها** (`kc_ai_history/admin` رجعت `null`) — نفس قاعدة [[project_zaitoon_academy]] بتنظيف أي اختبار Firebase حقيقي.
`tsc` + `build` نظيفان لكل الملفات السبعة المعدّلة/الجديدة.

## ✅ تصحيح الأوراق بالذكاء الاصطناعي — الأداة السابعة مكتملة (2026-09-03، نفس الجلسة)
`/ai-tools/paper-grading`: المعلم يرفع صورة ورقة الطالب (+ معيار تصحيح اختياري)، والذكاء الاصطناعي يقرأ الصورة فعلياً (حتى خط اليد) ويصحح كل سؤال ويعطي درجة إجمالية وملاحظة تشجيعية شخصية.
- **تحقّق تقني أولاً قبل البناء** (بمفتاح Groq حقيقي، صورة اختبار مُنشأة بـPython/PIL بإجابة خاطئة متعمّدة): أرسلت طلب curl مباشر لـ`qwen/qwen3.8-27b` بصيغة الرسائل متعددة الوسائط القياسية (`image_url` بصيغة `data:image/...;base64,...`) — النموذج قرأ الصورة بدقة تامة وصحّح، وحدّد الإجابة الخاطئة بشكل صحيح 100%، `finish_reason: stop` نظيف.
- أضفت `callGroqVision()` بـ`lib/groq.ts` (نفس النموذج، تنسيق OpenAI-compatible القياسي لمحتوى الصورة+النص معاً).
- الصفحة: رفع صورة (`<input type=file accept=image/*>` → `FileReader` → base64 بمتصفح المستخدم مباشرة، بدون أي رفع لسيرفر خارجي)، معاينة مصغّرة، حد أقصى 8 ميجا، نفس نمط حفظ/نسخ/طباعة/سجل أعمالي المستخدم بالأدوات الست.
- **اختُبرت حياً بالكامل عبر واجهة المستخدم الفعلية** (رفع ملف صورة حقيقي عبر أتمتة المتصفح، لا curl فقط): ورقة اختبار بها اسم "Sara" و4 مسائل حسابية (وحدة متعمّدة خاطئة: 9-3 كتبها الطالب 5 بدل 6) — النتيجة: قرأ الاسم بدقة، صحّح كل الأربع مسائل بدقة 100%، احتسب "3 من 4" بشكل صحيح، وكتب ملاحظة تشجيعية شخصية **بصيغة المخاطبة المؤنثة الصحيحة نحوياً** ("أحسنتِ يا سارة!... انتبهي... واصلي المذاكرة!") — جودة لغوية عالية فعلاً مو آلية.
- تحذير ظاهر بالنتيجة دائماً: "راجع دائماً تصحيح الذكاء الاصطناعي بنفسك قبل اعتماد الدرجة النهائية" — الأداة مساعدة للمعلم مو بديلة عنه.
- `tsc` + `build` نظيفان.

## 🐛 خطأ "ما اشتغل" في مولّد ورقة العمل — كان تجربة استخدام لا خطأ برمجي (2026-09-03)
محمد قال "حاولت اعمل ورقة نشاط لم تعمل". فحصت `/ai-tools/worksheet` حياً بعدة طرق (طلب مباشر لـGroq بنفس الـprompt، ثم ضغط الزر فعلياً بمتصفح حقيقي): **الأداة تشتغل صحيح 100%** — الطلب ينجح، والنتيجة تُرسم كاملة وصحيحة. المشكلة الحقيقية: **نتيجة التوليد تظهر أسفل الصفحة (تحت الفورم)، وما فيه أي تمرير تلقائي أو إشارة واضحة إنه "خلص وطلعت النتيجة تحت"** — فوقعت أنا نفسي بنفس اللبس أول مرة أثناء الفحص (ظننت إنه ما اشتغل لأن الشاشة ما تغيّرت، لين نزّلت بالصفحة ولقيت النتيجة كاملة جاهزة). هذا بالضبط الشعور اللي حصل مع محمد على الأغلب.
**الإصلاح المطبَّق فوراً بكل الأدوات السبع**: إضافة تمرير تلقائي (`scrollIntoView({behavior:"smooth"})`) لصندوق النتيجة بمجرد ظهورها — بمجرد ما يضغط المعلم زر التوليد وتجهز النتيجة، الصفحة تنزل تلقائياً لعرضها بدل ما يبقى المستخدم يتساءل "هل اشتغل ولا لا".
- `tsc` + `build` نظيفان بعد التعديل على كل الملفات السبعة.
- **قيد اختباري**: واجهت مشكلة بيئة أتمتة المتصفح (نافذة كروم صارت ضيقة جداً ~682px من محاولات `resize_window` سابقة بنفس الجلسة، وتعذّر إصلاحها لاحقاً — "Bounds must be at least 50% within visible screen space") فما قدرت أعيد تأكيد التمرير التلقائي بصرياً 100% بنفس الجلسة. لكن المنطق البرمجي بسيط وقياسي (`useEffect` يراقب حالة النتيجة وينفّذ `scrollIntoView` بمجرد ظهورها) وهذا النمط اشتغل بنجاح مؤكَّد بأماكن مشابهة هذي الجلسة — ثقة عالية جداً إنه يعمل صحيح، لكن يستاهل محمد يتأكد بنفسه أول استخدام قادم.

## 🔲 التالي
درس فيديو تفاعلي (الأعقد تقنياً — يحتاج تشغيل فيديو + إيقاف تلقائي بنقاط زمنية + توليد أسئلة سياقية، لم يُبدأ بعد).

## 📋 ملخص شامل لمركز أدوات الذكاء الاصطناعي (7 أدوات + مساعد ذكي + سجل أعمالي)
كلها بمفتاح Groq واحد مجاني محفوظ بالسحابة، نموذج موحّد `qwen/qwen3.8-27b`:
1. `/ai-tools/lesson-plan` — مولّد خطة الدرس ✅
2. `/ai-tools/question-generator` — مولّد الأسئلة ✅
3. `/ai-tools/worksheet` — مولّد ورقة العمل ✅
4. `/ai-tools/mind-map` — مولّد الخريطة الذهنية ✅
5. `/ai-tools/activity-generator` — مولّد الأنشطة الصفية ✅
6. `/ai-tools/competition-generator` — مولّد المسابقات الصفية ✅
7. `/ai-tools/paper-grading` — تصحيح الأوراق بالذكاء الاصطناعي (رؤية/صور) ✅
+ `/ai-assistant` — المساعد الذكي (محادثة حرة، موجود من قبل) ✅
+ `/ai-tools/history` — سجل أعمالي (حفظ/عرض/نسخ/طباعة/حذف لأي نتيجة من أي أداة) ✅
جميعها ظاهرة بالقائمة الجانبية للأدمن والمعلمين (المنسّقين). كل الأدوات اختُبرت حياً من طرفي (Claude) بنفس المفتاح الحقيقي، **لكن محمد شخصياً لسه ما جرّب ولا أداة بنفسه من واجهة الموقع الحقيقية** — يستاهل يفتح `/ai-tools` بنفسه ويجرب.

## آخر التحديثات (2026-08-30 و 2026-08-31)
- ✅ ميزة جديدة كاملة: استبانة "مواهب الطلاب" (`/interest-survey`) + بطاقة باركود مستقلة (`/survey-qr`) بهوية بصرية خاصة (وحدة الموهبة والنشاط الطلابي)، 3 مجالات (الروبوت والذكاء الاصطناعي / الموهبة والابتكار / المناشط الطلابية) بحد اختيارات مختلف لكل مجال، حقل الصف والشعبة (1-6)، تقرير طباعة مفروز حسب لوحة الإدارة.
- ✅ **الاستبانة أُرسلت فعلياً لأولياء الأمور وهي مصدر بيانات حي** — أي تعديل مستقبلي عليها يحتاج اختبار أحرص لأنه يمس ردوداً حقيقية قيد الجمع.
- 🔲 **باقٍ من قبل، لم يُحل بعد**: قاعدة أمان Firebase لسه فضفاضة (`auth != null` يسمح لأي زائر تقنياً بقراءة بيانات الطلاب) — الحل الحقيقي يحتاج حسابات دخول حقيقية لكل منسّق + على الأغلب ترقية Firebase لخطة مدفوعة (Blaze)، قرار معلّق بانتظار محمد.

## آخر التحديثات (2026-08-24)
- ✅ فحص شامل للمنصة كاملة + إصلاح كل الأعطال المكتشفة:
  - إصلاح حرج: تعديل بيانات المنسّق من بوابته ما كان يوصل لقاعدة البيانات إطلاقاً (يُحفظ بالجهاز فقط).
  - إصلاح حرج: 6 أقسام ببوابة المنسّق (الخطط، البرامج، المسابقات، الإنجازات، التطوير المهني، فرق الطلاب) كانت تحفظ محلياً بس بدون أي مزامنة — صارت تُزامن بالسحابة الآن.
  - إصلاح حرج: محادثة الجروبات ببوابة الطالب كانت نسخة منفصلة محلية فقط (لا تصل لباقي الأعضاء) — استُبدلت بنفس نظام المحادثة الحقيقي المستخدم بلوحة الإدارة (`components/shared/GroupsChat.tsx`).
  - إصلاح حرج: كلمات مرور الطلاب/المنسقين صارت مشفّرة (PBKDF2) بدل نص عادي — مع ترقية تلقائية للحسابات القديمة بأول دخول ناجح.
  - إصلاح: طالب مسجّل قبل نظام رمز الجهاز (فارس اللهيب) كان بلا طريقة رجوع للمنصة إطلاقاً (نظام الدخول القديم برقم الهوية وكلمة المرور صار غير مستخدم بالواجهة) — التسجيل بنفس رقم الهوية صار "يربط" الحساب القديم برمز جهاز جديد بدل رفضه.
  - إصلاح: ثغرة رمز تسجيل المنسّقين (لو تُرك فارغاً كان يقبل أي رمز).
  - إصلاح: أخطاء JS محتملة (مصفوفات صور/فيديو فاضية تُسقط الصفحة) بأقسام الأرشيف/المسابقات/البرامج/فرق الطلاب — نفس عطل اليوميات القديم.
  - حذف كود ميت (`components/student/TeamGroupChat.tsx`).
  - 🔲 **باقي على محمد**: قواعد أمان Firebase لسه فضفاضة (`auth != null`) — يحتاج يلصق قواعد جديدة بنفسه بكونسول Firebase، راجع تفاصيل الطلب الأخير بالمحادثة.

## آخر التحديثات (2026-08-17)
- ✅ إصلاح حرج: نظام الاجتماعات (`/meetings`) كان يحفظ محلياً فقط ولا يصل لباقي الأجهزة — صار يُزامن حياً عبر Firebase.
- ✅ ميزة جديدة: توقيع كل منسّق (من حسابه) على كل بند اجتماع، توثيقاً لالتزامه — `app/meetings/page.tsx`.
- ✅ إمكانية تعديل المعنيين ووصف كل محور بعد إنشاء الاجتماع مباشرة (للأدمن).
- ✅ ميزة جديدة كاملة: اختبار مهارات التفكير العليا `/talent-test` — جدول تاريخ+صف يديره الأدمن من تبويب "رموز التسجيل"، الطالب يفتحه بيومه بالضبط فقط، محاولة واحدة، والنتيجة تصل للأدمن مباشرة.
- ✅ اجتماع حقيقي جاهز لتاريخ 2026-08-18 بعنوان "اجتماع المنسقين — خطة المنافسات والمشاريع والروبوت" مع 4 محاور وقرارات كاملة من عرض محمد.
- 🔲 **باقي على محمد**: تحديد تواريخ جدول اختبار مهارات التفكير العليا الفعلية من لوحة الإدارة (اقتُرحت عليه تواريخ مبدئية بالمحادثة، لم تُدخل بعد).
- ✅ كلمات المرور صارت مشفّرة (2026-08-24، راجع تحديثات اليوم أعلاه). 🔲 لسه باقٍ: قاعدة أمان Firebase فضفاضة (`auth != null` يسمح لأي زائر بالقراءة).

## معلومات المشروع
- **الاسم:** مركز المعرفة والابتكار
- **التقنية:** Next.js 14 + TypeScript + Tailwind CSS
- **الاتجاه:** RTL عربي كامل
- **GitHub:** zaytoon304/knowledge-center
- **المسار:** /Users/mac/knowledge-center

## التقنيات المستخدمة
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS (RTL)
- Recharts (رسوم بيانية)
- Lucide React (أيقونات)
- clsx (conditional classes)

## هيكل المشروع
```
knowledge-center/
├── app/
│   ├── layout.tsx          ✅
│   ├── page.tsx            ✅ الصفحة الرئيسية
│   ├── globals.css         ✅
│   ├── knowledge/page.tsx  ✅ مركز المعرفة
│   ├── programs/page.tsx   ✅ مركز البرامج
│   ├── projects/page.tsx   ✅ مركز المشاريع
│   ├── training/page.tsx   ✅ مركز التدريب
│   ├── indicators/page.tsx ✅ مركز المؤشرات
│   ├── emerging-tech/page.tsx ✅ التقنيات الناشئة
│   ├── ai-assistant/page.tsx ✅ المساعد الذكي
│   ├── portfolio/page.tsx  ✅ الملف المهني
│   ├── competitions/page.tsx ✅ المسابقات
│   ├── student-portal/page.tsx ✅ بوابة الطلاب
│   ├── project-bank/page.tsx ✅ بنك المشاريع
│   └── admin/page.tsx      ✅ لوحة الإدارة
├── components/
│   ├── layout/
│   │   ├── Navbar.tsx      ✅
│   │   ├── Sidebar.tsx     ✅
│   │   └── Footer.tsx      ✅
│   └── ui/
│       ├── StatCard.tsx    ✅
│       ├── SectionCard.tsx ✅
│       └── Badge.tsx       ✅
├── data/
│   ├── stats.ts            ✅
│   ├── projects.ts         ✅
│   ├── courses.ts          ✅
│   ├── competitions.ts     ✅
│   ├── programs.ts         ✅
│   └── knowledge.ts        ✅
└── public/

## الأقسام الفعلية الحالية (34 قسم، تحقّقت من `app/` مباشرة بتاريخ 2026-09-01)
- [x] الصفحة الرئيسية
- [x] مركز المعرفة
- [x] مركز البرامج
- [x] مركز المشاريع
- [x] مركز التدريب (دورات محمية بعرض شرائح)
- [x] مركز المؤشرات
- [x] التقنيات الناشئة
- [x] المساعد الذكي
- [x] الملف المهني (Portfolio)
- [x] مركز المسابقات والجوائز
- [x] بوابة الطلاب والطالبات
- [x] بنك المشاريع
- [x] لوحة الإدارة
- [x] تسجيل الدخول (طالب / منسّق / أدمن)
- [x] بوابة المنسّق (Coordinator Portal)
- [x] بوابة ولي الأمر (Parent Portal)
- [x] تسجيل الزوار (Visitor + Visitor Portal)
- [x] البث المباشر (Live)
- [x] السبورة التفاعلية بالبث (Whiteboard)
- [x] الاجتماعات (Meetings)
- [x] اليوميات (Daily Log)
- [x] مساحة الحديث الجماعية (Groups) — نص + صور + ملفات + صوت + فيديو
- [x] المعرض (Gallery)
- [x] لوحة الصدارة (Leaderboard)
- [x] الشهادات (Certificates)
- [x] اختبار الذكاء (IQ Test)
- [x] اختبار مهارات التفكير العليا (Talent Test)
- [x] تتبع المشاريع (Project Tracking)
- [x] هيا نتعلم (Lets Learn)
- [x] نلعب ونتعلم (Play)
- [x] الإشراف (Oversight)
- [x] المشرف (Supervisor)
- [x] باركود المنصة (Platform QR)
- [x] استبانة مواهب الطلاب (Interest Survey) — حية، تستقبل ردود حقيقية من أولياء الأمور
- [x] بطاقة باركود الاستبانة (Survey QR)

> ⚠️ لو رجعت لهذا الملف لاحقاً: تحقّق من `app/` مباشرة قبل الاعتماد على هذي القائمة — أقسام جديدة ممكن تُضاف بدون تحديث هذا الملف فوراً.
